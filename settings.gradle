plugins {
    // Let Gradle provision the JDK toolchain when none is on the host (e.g. CI).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'coding-training'
